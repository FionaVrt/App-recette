# 🎨 Architecture Visuelle

## App Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                   MES RECETTES APP                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ index.html (PWA Shell)                              │  │
│  │ - Meta tags PWA                                     │  │
│  │ - Service Worker register                          │  │
│  │ - Manifest link                                    │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                           │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │ src/main.jsx                                        │  │
│  │ - React root                                        │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                           │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │ src/App.jsx                                         │  │
│  │ - Wrapper component                                 │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                           │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │ src/RecipeSaver.jsx (Main Component)                │  │
│  │                                                      │  │
│  │  onAuthChange() → user state                        │  │
│  │       │                                             │  │
│  │       ├─ if (authLoading) → Loading spinner        │  │
│  │       │                                             │  │
│  │       ├─ if (!user) → AuthPage (login/signup)      │  │
│  │       │                    │                        │  │
│  │       │                    └─ loginUser()           │  │
│  │       │                    └─ registerUser()        │  │
│  │       │                                             │  │
│  │       └─ if (user) → Main App                       │  │
│  │                      │                              │  │
│  │                      ├─ Header (user + logout)     │  │
│  │                      ├─ Tabs (Recipes/Cocktails)   │  │
│  │                      ├─ RecipeList/CocktailList    │  │
│  │                      └─ CookingMode                │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                           │
│  ┌──────────────▼───────────────────────────────────────┐  │
│  │ Services Layer                                      │  │
│  │                                                      │  │
│  │  ┌─ src/authService.js                             │  │
│  │  │  ├─ registerUser(email, pwd)                     │  │
│  │  │  ├─ loginUser(email, pwd)                        │  │
│  │  │  ├─ logoutUser()                                 │  │
│  │  │  └─ onAuthChange(callback)                       │  │
│  │  │                                                   │  │
│  │  ├─ src/firebaseService.js                         │  │
│  │  │  ├─ loadRecipesFromFirebase(userId)             │  │
│  │  │  ├─ addRecipeToFirebase()                        │  │
│  │  │  ├─ updateRecipeInFirebase()                     │  │
│  │  │  ├─ deleteRecipeFromFirebase()                   │  │
│  │  │  ├─ [Same for Cocktails]                         │  │
│  │  │                                                   │  │
│  │  └─ src/firebase.js                                │  │
│  │     ├─ initializeApp()                              │  │
│  │     ├─ export db (Firestore)                        │  │
│  │     └─ export auth (Auth)                           │  │
│  └──────────────┬───────────────────────────────────────┘  │
│                 │                                           │
└─────────────────┼───────────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
  ┌──────────────┐    ┌──────────────┐
  │   Firebase   │    │   Service    │
  │ Auth Service │    │   Worker     │
  │              │    │              │
  │ Auth checks  │    │ Offline      │
  │ Sessions     │    │ Cache        │
  └──────────────┘    └──────────────┘
        │                   │
        ▼                   ▼
  ┌──────────────┐    ┌──────────────┐
  │   Firestore  │    │  localStorage│
  │   Database   │    │ (persistence)│
  │              │    │              │
  │ /users/{id}/ │    │ darkMode     │
  │  recipes     │    │ (if legacy)  │
  │  cocktails   │    └──────────────┘
  └──────────────┘
```

---

## Data Structure Tree

```
FIRESTORE DATABASE
│
└── users/
    │
    ├── {userId-1}/                    ← Each user's data
    │   ├── recipes/
    │   │   ├── {recipeId-1}
    │   │   │   ├── title: "Pâtes"
    │   │   │   ├── url: "https://..."
    │   │   │   ├── ingredients: [...]
    │   │   │   ├── category: "plat"
    │   │   │   ├── favorite: true
    │   │   │   ├── note: "Délicieux!"
    │   │   │   ├── prepTime: 30
    │   │   │   └── timestamp: 2024-01-15
    │   │   │
    │   │   └── {recipeId-2}
    │   │       └── ...
    │   │
    │   └── cocktails/
    │       ├── {cocktailId-1}
    │       │   ├── name: "Mojito"
    │       │   ├── type: "avec-alcool"
    │       │   ├── ingredients: [...]
    │       │   ├── favorite: false
    │       │   └── timestamp: 2024-01-14
    │       │
    │       └── {cocktailId-2}
    │           └── ...
    │
    └── {userId-2}/                    ← Another user's data
        ├── recipes/
        │   └── (only this user's recipes)
        │
        └── cocktails/
            └── (only this user's cocktails)
```

---

## Authentication Flow

```
USER VISIT
   │
   ▼
┌─────────────────────────┐
│ onAuthChange() listener │
│ (RecipeSaver useEffect) │
└────┬────────────────────┘
     │
     ├─ Loading... ───► Show Spinner
     │
     ├─ User === null
     │     │
     │     ▼
     │  ┌──────────────────────┐
     │  │   AuthPage.jsx       │
     │  │                      │
     │  │  Email: [_______]    │
     │  │  Pass:  [_______]    │
     │  │                      │
     │  │  [Sign Up]  [Login]  │
     │  └────────┬─────────────┘
     │           │
     │           ├─ registerUser()
     │           │  └─ Firebase Auth ✅
     │           │
     │           └─ loginUser()
     │              └─ Firebase Auth ✅
     │                  │
     │                  ▼
     │            localStorage stores
     │            session (persistence)
     │
     └─ User !== null
          │
          ▼
       ┌──────────────────────┐
       │  Main App Rendered   │
       │                      │
       │  [🚪 user@email] ◄─── Logout button
       │  [🌙] Dark Mode      │
       │                      │
       │  Tabs:               │
       │  [Recipes] [Cocktails] [Add]
       │                      │
       │  → firebaseService   │
       │    loads user's data │
       └──────────────────────┘
```

---

## File Dependencies

```
index.html
    │
    └─► main.jsx
         │
         ├─► App.jsx
         │    │
         │    └─► RecipeSaver.jsx ◄────┐
         │         │                    │
         │         ├─► AuthPage.jsx ────┤
         │         │    │               │
         │         │    └─► authService.js
         │         │         │
         │         │         └─► firebase.js
         │         │
         │         ├─► firebaseService.js
         │         │    │
         │         │    └─► firebase.js
         │         │
         │         └─► RecipeCard.jsx (subcomponent)
         │
         └─► CSS (index.css)
              │
              ├─► tailwind.config.js
              │
              └─► postcss.config.js
                  │
                  └─► tailwindcss plugin

public/
    ├─► manifest.json (PWA)
    │
    ├─► sw.js (Service Worker)
    │
    └─► Icons (apple-touch-icon, etc)
```

---

## Component Hierarchy

```
<App>
    └── <RecipeSaver>
         │
         ├── Show Spinner (if authLoading)
         │
         ├── Show <AuthPage> (if !user)
         │    ├── Email Input
         │    ├── Password Input
         │    ├── Sign Up / Login Button
         │    └── Toggle Sign Up ↔ Login
         │
         └── Show Main App (if user)
              │
              ├── Header
              │   ├── Logo "Mes Recettes"
              │   ├── Dark Mode Toggle 🌙
              │   └── Logout Button 🚪
              │
              ├── Tabs
              │   ├── [Recipes Tab]
              │   ├── [Cocktails Tab]
              │   └── [Add Recipe/Cocktail Tab]
              │
              ├── Content Area
              │   ├── If Recipes Tab:
              │   │   ├── Search Bar
              │   │   ├── Category Filter
              │   │   └── RecipeList
              │   │       └── <RecipeCard> (repeating)
              │   │           ├── Star (favorite)
              │   │           ├── Share Button
              │   │           ├── Copy Button
              │   │           └── Delete Button
              │   │
              │   ├── If Cocktails Tab:
              │   │   ├── Category Filter (alcool/sans/mocktail)
              │   │   └── CocktailList
              │   │       └── <CocktailCard> (repeating)
              │   │
              │   └── If Add Tab:
              │       ├── URL Input Field
              │       └── Add Recipe Button
              │
              ├── Toast Notifications (absolute)
              │
              └── Cooking Mode (if enabled)
                  ├── Fullscreen recipe view
                  ├── Ingredient checkboxes
                  └── Portion adjuster
```

---

## Security Model

```
┌──────────────────────────────────────────┐
│         SECURITY LAYERS                  │
└──────────────────────────────────────────┘

LAYER 1: Authentication
┌────────────────────────────────────────┐
│ Firebase Authentication                │
│ - Email/Password validation            │
│ - Session management                   │
│ - Login persistence                    │
│ - Logout capability                    │
└────────────────────────────────────────┘
         │
         ▼
LAYER 2: Authorization
┌────────────────────────────────────────┐
│ Firestore Security Rules (To Apply!)   │
│                                        │
│ match /users/{userId} {                │
│   allow read, write:                   │
│   if request.auth.uid == userId;       │
│ }                                      │
└────────────────────────────────────────┘
         │
         ▼
LAYER 3: Data Isolation
┌────────────────────────────────────────┐
│ Database Structure                     │
│                                        │
│ /users/{userId}/recipes/               │
│ /users/{userId}/cocktails/             │
│                                        │
│ Each user ↔ Own data only              │
└────────────────────────────────────────┘

Result: ✅ Each user sees ONLY their data
        ✅ No one can access others' data
        ✅ Data encrypted in transit
        ✅ Passwords hashed by Firebase
```

---

## Build Pipeline

```
SOURCE CODE
    │
    ├─ src/*.jsx, *.js
    │
    ├─ src/index.css
    │  ├─ @tailwind directives
    │  └─ PostCSS processing
    │
    └─ index.html
         │
         ▼
    VITE BUNDLER
         │
         ├─ React plugin
         ├─ Fast refresh
         └─ Hot module replacement
         │
         ▼
    TAILWIND CSS
         │
         ├─ Scan src/** for classes
         ├─ Generate CSS only for used classes
         └─ PurgeCSS optimization
         │
         ▼
    WEBPACK BUNDLING
         │
         ├─ Code splitting
         ├─ Tree shaking
         └─ Minification
         │
         ▼
    OUTPUT (dist/)
         │
         ├─ dist/index.html (0.99 kB)
         ├─ dist/assets/index-*.css (20.22 kB)
         └─ dist/assets/index-*.js (650.10 kB)
         │
         ▼
    GZIP COMPRESSION
         │
         ├─ CSS: 4.27 kB
         └─ JS: 166.40 kB
         │
         ▼
    NETLIFY DEPLOYMENT
         │
         └─ https://resonant-alfajores-b439bf.netlify.app/
```

---

**Visualizations created! 🎨**
